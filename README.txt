Drop real audio files here with these exact names and the site will pick them up automatically:

  library-ambience.mp3   — a quiet, looping room-tone / library ambience
  book-open.mp3          — a short cover-opening sound
  book-close.mp3         — a short cover-closing sound
  page-turn.mp3          — a short page-flip sound

Until these files exist, the site runs silently — all playback calls are
wrapped safely so missing audio never breaks the experience.
